# Skills cull — 2026-09-07 (S2a STRUCTURE)

Executed on branch `conductor`. Manifest was supervisor-reviewed (S1
classification); this log records what moved, what merged, what was scrubbed.
Skill counts: 82 folders before → **53 shipped** after, 1:1 with the rebuilt
`soul/skills/INDEX.md`.

## Bucket 1 — CASE → `examples/hunts/<category>/<name>/` (2)

Whole one-target hunts: narrative + case-specific references, kept as worked
examples, no longer shipped skills.

| Skill | From → To | Reason |
|---|---|---|
| epixnet-ui-redteam | web2/ → examples/hunts/web2/ | F1–F9 one-target narrative with lib.rs:3164 / command.rs:351 line numbers + live PoC deposit — a case, not a class. |
| cdc-meraki-bug-bounty | cdc/ → examples/hunts/cdc/ | One-target hunt directive + session findings; its class-CDC portion is duplicated in cdc-web-app-bug-bounty. |

## Bucket 2 — ATTIC → `soul/skills-attic/<category>/<name>/` (22)

Real methodology preserved verbatim, outside the operator's current lane
families. Contract in `soul/skills-attic/README.md`.

| Skill | From → To | Reason |
|---|---|---|
| 0g-storage-node-audit | web3/ → attic/web3/ | Niche node family outside current lane families (sharp content, preserved). |
| blockchain-consensus-audit | web3/ → attic/web3/ | Consensus-layer family outside current lanes. |
| consensus-integration-audit | web3/ → attic/web3/ | Consensus-glue family outside current lanes. |
| consensus-protocol-audit | web3/ → attic/web3/ | Consensus-design family outside current lanes. |
| cosmos-evm-deserialization-audit | web3/ → attic/web3/ | Cosmos/EVM niche surface. |
| cosmos-hook-audit | web3/ → attic/web3/ | Cosmos module-hook niche surface. |
| cosmos-live-chain-verification | web3/ → attic/web3/ | Cosmos live-chain niche surface. |
| cosmwasm-contract-audit | web3/ → attic/web3/ | CosmWasm niche surface. |
| go-edge-case-audit | web3/ → attic/web3/ | Go node edge-case family outside current lanes. |
| hypervisor-trust-boundary-audit | web3/ → attic/web3/ | Hypervisor family outside current lanes. |
| kubernetes-security-audit | web3/ → attic/web3/ | K8s family outside current lanes. |
| octopus-deploy-audit | web3/ → attic/web3/ | Octopus Deploy family outside current lanes. |
| rust-l1-node-audit | web3/ → attic/web3/ | Rust L1 node family outside current lanes. |
| solana-anchor-audit | web3/ → attic/web3/ | Solana family outside current lanes. |
| spring-oauth-rbac-gate-audit | web3/ → attic/web3/ | Spring/Envoy gate family outside current lanes. |
| spring-rbac-oauth-audit | web3/ → attic/web3/ | Spring RBAC/OAuth family outside current lanes. |
| wasm-vm-sandbox-audit | web3/ → attic/web3/ | WASM VM family outside current lanes. |
| alpenglow-destroy-button-methodology | cdc/ → attic/web3/ | Consensus-protocol family (web3-adjacent; moved from its ACTUAL on-disk category cdc/). Generic payload already lives in adversarial-bug-bounty-hunting. |
| web2-admin-cdc | web2/ → attic/web2/ | Derivative variant of web2-admin-takeover (kept as canonical). |
| web2-admin-hunt | web2/ → attic/web2/ | Derivative variant of web2-admin-takeover (kept as canonical). |
| web-audit-report-delivery | cdc/ → attic/cdc/ | Report format is the partner's standard (plans.md P8 refusal); delivery discipline returns when Phase A reopens. |

## Bucket 3 — RUNBOOK → `docs/operator/<name>/` (6)

Operator infrastructure/runbook content, not hunting methodology. Scrubbed
during the move (see Scrubs).

| Skill | From → To | Reason |
|---|---|---|
| cloud-instance-hardening | osint/ → docs/operator/ | Own-cloud hardening runbook. |
| headless-shell-access | osint/ → docs/operator/ | Agent-tooling/shell diagnostics runbook. |
| hermes-config-management | osint/ → docs/operator/ | Hermes provider/config management runbook. |
| session-hygiene | osint/ → docs/operator/ | Operator session ops runbook. |
| vps-audit | osint/ → docs/operator/ | Own-VPS audit runbook. |
| vps-cleanup | osint/ → docs/operator/ | Post-hunt VPS cleanup runbook. |

## Bucket 4 — DEDUP (2 merges + 1 deletion)

| Action | Detail |
|---|---|
| cdc-multi-target-audit → merged + moved | Unique content (the copy-paste agent-instruction template + the cumulative Pitfalls ledger, incl. the plasma/ethrex and 2026-08-15/16 pitfall entries) appended to `cdc/multi-target-cdc-audit/SKILL.md` under a marked section `## From cdc-multi-target-audit (merged 2026-09-07)`. The whole folder then moved to `examples/hunts/cdc/cdc-multi-target-audit/` as a case. |
| nextauth-authz-probe → merged + moved | Unique runnable script `nextjs-middleware-bypass-test.py` moved (git mv) into `web2/nextjs-authn-authz-probing/scripts/`. `antrp-pickle-rce-generator.py` intentionally NOT merged (case-weaponized; travels with the folder). The whole folder then moved to `examples/hunts/web2/nextauth-authz-probe/`. The broken `scripts/nextauth-probe.sh` reference in nextjs-authn-authz-probing/SKILL.md (file never existed) was removed and the Scripts section now points at the merged script. |
| osint/github-secret-hunt → DELETED | Verified byte-identical twin of web2/github-secret-hunt (`diff -r` clean). `git rm -r`; canonical stays web2. |

## Scrubs (applied during RUNBOOK + DEDUP moves)

| Target | Action |
|---|---|
| hermes-config-management (SKILL.md + 7 references, 76 replacements) | All `<internal-endpoint>` endpoints (and bare `vm-0-3` host prefixes) → `<internal-endpoint>`; all elided/partial APIKU keys (`<redacted>`, `<redacted>`, `<redacted>`) and display-truncation key shapes (`<redacted>`) → `<redacted>`. Also found and redacted 2 FULL live TokenHarbor keys (`<redacted>`, `<redacted>` in references/tokenharbor-key-rotation-402-2026-08-11.md and tokenharbor-2026-08-11-ghost-model.md) plus their elided forms (`<redacted>`, `<redacted>`, `<redacted>`) → `<redacted>`. Provider names and public docs left intact. **ROTATION ADVISED: treat the TokenHarbor and APIKU keys as live — they existed in git history; rotating them is the operator's job.** |
| cdc-multi-target-audit/references/plasma-ethrex-audit-2026-08.md (5 redactions) | Raw hex private keys SPONSOR `<redacted-private-key-rotated>` (options.rs:44/55), COMMITTER `<redacted-private-key-rotated>` (:661), PROOF_COORD `<redacted-private-key-rotated>` (:795), and the OnChainProposer OWNER key `<redacted-private-key-rotated>` (deployer.rs:426 comment + table row) → `<redacted-private-key-rotated>`, done BEFORE the folder moved to examples. Public addresses kept. **ROTATION ADVISED: these keys were proven signable with mainnet nonces — treat as live; rotation/monitoring is the operator's job.** |
| Scope note | Nothing else was scrubbed in this pass; S2b strips remaining case-content residue inside examples/hunts/. |

## Ghost entries removed from INDEX.md

- `nextauth-email-enum` — no folder on disk.
- `adversarial-exploit-chainers` — no folder; real skill is `adversarial-exploit-chains` (lane loadout re-pointed to it).
- `bridge-audit` — no folder on disk.
- `ec2-instance-hardening` — no folder on disk.
- `wasm-vm-sandbox-escape-audit` — no folder on disk (the real `wasm-vm-sandbox-audit` moved to the attic).
- web3 `alpenglow-destroy-button-methodology` pointer line ("see cdc/") — folder moved to attic/web3/.
- web3 `wasm-vm-sandbox-audit` line — folder moved to attic.
- `cdc-blockchain-audit` and `rate-limited-chain-recon` lines were checked against disk and KEPT (folders still shipped in web3/).
- osint category reduced to exactly one skill (`infrastructure-osint`); `github-secret-hunt` listed under web2 only.
- Lane loadouts: Red-Teamer `nextauth-authz-probe` → `nextjs-authn-authz-probing`; Chainer `adversarial-exploit-chainers` → `adversarial-exploit-chains`. Verifier "NOT a dispatched lane" annotation unchanged.

## New structure contract

- **53 shipped skills** across six categories: recon 4, web2 19, web3 18, cdc 5, verification 6, osint 1.
- `soul/skills/INDEX.md` rebuilt as the allow-list — one bullet per skill, 1:1 with disk.
- `app/tests/test_skills.py` extended: (1) INDEX bullets biject 1:1 with skill folders (no ghosts, no unlisted, no duplicate placement); (2) every SKILL.md opens with `---` and its frontmatter `name:` equals the folder name (extends the existing name/description/single-line lint); (3) `skill_view` refs in SOUL.md are strictly resolved — no catalogued-but-missing tolerance.
- Edge-fixes applied to KEEP skills: `black-swan-engine` frontmatter fence added (opening + closing `---`, content otherwise untouched); `web-blocked-page-recovery` frontmatter name aligned to folder; `adversarial-bug-bounty-hunting` 10 stray `# Reference:` comment lines stripped from the frontmatter block only.
- CI "Skills reference integrity" checked post-cull: the single SOUL.md ref (`black-swan-engine`) still resolves — no CI breakage.

## Known residue (out of scope, for the record)

- `app/WALKTHROUGH.md` line ~222 shows an illustrative `skill_view(name='cdc-multi-target-audit')` example; that skill moved to examples/hunts/. Not enforced by CI/tests (only SOUL.md is); flagged to the supervisor, not edited in this pass.

## Addendum — supervisor scrub (post S2b follow-ups)

- `soul/skills-attic/cdc/web-audit-report-delivery/references/wyze-poc2-forge.md`:
  the 3 live Wyze HMAC-MD5 signing secrets (all forms incl. the split-string
  concatenations at lines 38-40) redacted by the supervisor after both content
  agents flagged them out-of-scope. Repo-wide sweep for all four shapes
  (gbJojEB* / obb2ZnAz* / 5Z1Bg7wM* / WMNpD2qNPC*): zero copies remain outside
  this log's 8-char identification prefixes (kept deliberately so the operator
  can match rotated secrets).
- NEW live keys found during the content passes (all redacted; ALL PREDATE THIS
  COMMIT IN GIT HISTORY — ROTATION ADVISED): 2x full TokenHarbor keys
  (thk_live_*), 5x plasma hex private keys (proven signable), Keeta funded
  wallet ed25519 seed_hex (proven signable, 2 files), go-abey devnet 64-hex
  private key, 2x MultiVAC BIP39 mnemonics + 11x 128-hex keys, Wyze HMAC x3 +
  OAuth clientSecret, ED3 apiKey x2, Naoris admin/Firebase passwords, 375.ai
  devnet keypairs. Full details in cull-2026-09-07-content-A.md / -B.md.

## Identity sweep (2026-09-07, operator directive: the repo is going general)

- Handles `the operator` / `the operator` / `persona` / `the operator` and Telegram chat id `<redacted>`
  scrubbed repo-wide (working tree): 3 shipped-skill genericizations, 1 shipped
  skill moved to the attic (adversarial-persona-operations — the persona
  dual-persona system), 2 operator persona docs DELETED (persona-config.md,
  direct-execution-protocol.md — recoverable from git history), 12
  docs/operator files scrubbed, 18 examples/hunts replacements, 49 pr/log
  replacements.
- SOUL.md: one honorific phrase -> "the operator" (line ~161); the full
  CRUISE/WAR doctrine rewrite remains a separate operator decision.
- Guards: test_skills now forbids all four handles in landed skills;
  test_identity.py pins the whole working tree (tracked files) via git
  ls-files. The earlier "retain the author handle" decision
  (recon-web2-osint-hygiene.md) is reversed.
- NOT covered: git history — every commit before this one still carries the
  operator's author identity (fazaxxx11 email) and pre-sweep handles; a
  history rewrite is the operator's separate call before any public release.
