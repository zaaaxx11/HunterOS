# 375ai API Enumeration — 2026-08-12 (Webflow vs Next.js Triage)

Validated against 375.ai org: 1 repo program-library, 3 web hosts.

## Host Triage
- `www.375.ai` = Webflow static (data-wf-domain, cdn.prod.website-files.com, 13 scripts jquery/webflow/gsap/recaptcha/Turnstile) — NO API surface. Robots empty, sitemap 404.
- `edge.375.ai` = Next.js 15 chunks, /login?redirect=/, vendor-only (turbopack/webpack runtime) — no app API client. /api/health returns 200 HTML login (not JSON), /api/auth/* 404.
- `app.375.ai` = Next.js 44 chunks, real client in `4554-421017f17b906b9e.js` (11KB, module 19861). `api.375.ai` discloses health.
- `api.375.ai` = NestJS (Cannot GET /), /health 200 leak.

## Health Leak (live)
```
GET https://api.375.ai/health → 200 application/json
{ status: ok, info: { db: up, redis-auth: up, datadb-user-data: up, inngest: up, app: up } }
x-request-id, vary: Origin, access-control-allow-credentials: true
```
Root / → 404, /openapi.json /docs /swagger 404 (not public).

## Axios Client (4554 / module 19861)
```js
baseURL: "https://api.375.ai", timeout: 60000,
headers: {"Content-Type":"application/json","X-Platform":"web","X-App":"375web/1", Authorization: "Bearer <auth>"}
refresh: POST /auth/token-refresh {refreshToken} {skipAuthRefresh:true} → {token, refreshToken} + interceptor queue (401 -> retry)
```

## Full Endpoint Inventory (from 4554 blocks 44554+78219)

Auth (/auth + /users):
GET /auth/is-registered?username-or-email=  (400 zod validation if empty — enumeration oracle)
POST /auth/sign-up, /auth/sign-in, /auth/sign-in-google, GET /auth/sign-in-solana, POST /auth/sign-in-solana/complete, GET /auth/magiclink/is-allowed, POST /auth/token-refresh, POST /auth/logout, POST /auth/forgot-password, POST /auth/reset-password, POST /auth/confirm-email, PATCH /users/profile, GET /users/profile, etc. /auth/change-username, /auth/change-password, /auth/generate/username, POST /auth/link-wallet, POST /auth/link-wallet/complete, GET /auth/sessions, DELETE /auth/sessions/{id}

Devices a="/devices":
GET /devices, GET /devices/{id}, GET /devices/stats, GET /devices/registration-wallet, POST /devices/registration-intent, POST /devices/register, POST /devices/authorize, GET /devices/status/{deviceKey}

Rewards f="/rewards":
GET /rewards/settings, /rewards/status, /rewards/stats, GET /rewards?{params}, GET /rewards/epochs/{id}/claim, GET /rewards/epochs/{id}/breakdown, POST /rewards/claimed

Maps/Missions/Leaderboard:
GET /maps/network-scout-hex, GET /missions/outlets/{id}, GET /missions/history/outlets, GET /missions/history/outlets/{id}/images, GET /leaderboard

Search K="/search":
GET /search/autocomplete?q=, /search/device-details/{id}, /search/user-details/{id}, /search/wallet-details/{id}, /search/wallet-devices

Users I="/users":
GET /users/activation-status, /users/waitlist-position, /users/wallet-details, POST /users/invite, GET /users/invitation/{id}, etc. /users/notification-preferences, /users/unsubscribe, /users/email

Payments: POST /payments/helio/charge

## Extraction Recipe (Hermes curl hardline fallback)
- HTML chunk discovery: `re.findall(r'/_next/static/chunks/[^"]+\.js', fetch(html))` — edge 15 vs app 44 (app holds real client)
- Must fetch BOTH hosts separately; edge vendor chunks miss app logic.
- Fallback when `curl` is BLOCKED (hardline): python urllib with User-Agent Mozilla/5.0 + gzip decompress. Validated same host headers.
- Live probe: `GET /auth/is-registered` with empty param → 400 proves validation (not 404).

## IDOR Surface (next phase)
Priority for fuzz (0.8s throttle, headers X-Platform/X-App): /devices/{id}, /devices/status/{deviceKey}, /rewards/epochs/{id}/claim, /search/wallet-details/{id}, /missions/history/outlets/{id}/images
